<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>CINZA</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>

	<!--
		Permission usage descriptions.

		Each string is shown verbatim in the iOS permission dialog, so it states
		what CINZA does with the access and nothing more. A vague or copied string
		is a common App Store rejection, and asking for a permission the app never
		uses (microphone, contacts, location) is another.
	-->
	<key>NSCameraUsageDescription</key>
	<string>CINZA uses the camera so you can photograph a scene and identify the film or series.</string>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>CINZA reads a screenshot you choose so it can identify the film or series in it.</string>
	<key>NSPhotoLibraryAddUsageDescription</key>
	<string>CINZA can save a still from an identified title to your photo library.</string>

	<!--
		App Transport Security.

		NSAllowsLocalNetworking permits a DEBUG build to talk to a plain-http dev
		server on the local network (see Settings > This device > API endpoint).
		It does not weaken TLS for anything else: production traffic is https.

		REMOVE THIS BLOCK before submitting to the App Store.
	-->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>

	<!-- The app uses only standard HTTPS, which is exempt from export compliance.
	     Declaring it here avoids the prompt on every TestFlight upload. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
</dict>
</plist>
